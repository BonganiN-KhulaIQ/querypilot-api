{
  "name": "querypilot-api",
  "private": true,
  "version": "0.1.0",

  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vercel/node": "^13.0.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
